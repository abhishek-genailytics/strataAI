# Core MVP dependencies
fastapi
uvicorn[standard]
pydantic[email]
pydantic-settings
python-dotenv
python-multipart

# HTTP client
httpx

# Monitoring and logging
structlog

# Supabase
supabase
gotrue

# Security
cryptography

# Additional dependencies (kept for compatibility)
aiohttp

# Database and ORM (kept for existing functionality)
sqlalchemy
alembic
psycopg2-binary

# Authentication and security (kept for existing functionality)
python-jose[cryptography]
passlib[bcrypt]
python-decouple

# Redis (kept but will be disabled in MVP)
redis
aioredis

# Monitoring and logging (kept for existing functionality)
prometheus-client

# Testing
pytest
pytest-asyncio

# Additional dependencies for gRPC and other features (kept for existing functionality)
grpcio
grpcio-tools
protobuf

# Development and debugging
ipython
black
flake8
mypy

# Documentation
mkdocs
mkdocs-material

# Environment and deployment
gunicorn
docker

# Additional utilities
python-dateutil
pytz

# Database async driver
asyncpg>=0.29.0

# Optional precise tokenizers (enable later if needed)
# tiktoken==0.7.0           # OpenAI models
# anthropic-tokenizer==0.1  # Claude models (if a lib is published/usable)